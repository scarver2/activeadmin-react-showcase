# config/initializers/content_security_policy.rb
# frozen_string_literal: true

# A production Content Security Policy is intentionally deferred until the
# showcase's embedded chart and editor libraries are selected. Security headers
# and the policy rollout checklist live in docs/security.md.
